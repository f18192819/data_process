# 第二周最终成果与生成脚本

此目录保存第二周的定稿题目材料、结构化结果、质量报告、生成脚本、测试和必要的人工审阅配置。

- `data/processed/` 是最终结构化输出；`reports/` 是质量审查和逐题分析结果。
- `scripts/` 包含转写和 trace 生成流程；`tests/` 包含对应测试。
- `configs/` 保存题库和人工审阅标注，供生成流程使用。

原始学生录音、原始资料压缩包，以及 `data/raw/`、`data/interim/`、日志和缓存未上传。完整重跑转写流程需要在本地另行提供这些原始输入。
